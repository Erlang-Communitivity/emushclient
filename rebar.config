%%-*- mode: erlang -*-
{erl_opts, [debug_info]}. %, fail_on_warning]}.

{lib_dirs,["deps"]}.

{escript_emu_args, "%%! -smp auto\n"}.

{require_otp_vsn, "R15"}.

{deps_dir, ["deps"]}.

{globals, [
	   {skip_deps, "false"}
	  ]}.

{deps,
  [
  ]}.
